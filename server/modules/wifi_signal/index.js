const workingAreas = [];
const restingAreas = [];

export function wifiSignalData2Location(data = []) {
  return "HKU Space";
}

export function isAtRestingSites(location) {
  return restingAreas.includes(location);
}

function sortBySignalStrength(data = []) {}

function findLocation(data = []) {}
